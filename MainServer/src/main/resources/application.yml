# HTTP (tomcat) port no 
server:
  port: 2345
  
# Configure this Discovery/registry server
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

